# リポジトリ直下に .refscan.conf として置く（zsh で source される）
# 追加の探索先
EXTRA_TARGETS+=("$HOME/.codex")
# 探索先ごとの追加除外
CODEX_EXCLUDES=(--exclude-dir=sessions --exclude-dir=logs --exclude='*.log')
REPO_PRUNE=(--exclude-dir=session)
# 「記録・証跡」パス（前方一致）。現役判定から除く。
RECORD_PATTERN='^(docs/archive/|logs/|worklog/)'
